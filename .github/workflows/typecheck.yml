name: Expo App TypeScript typecheck

on:
    pull_request:
        paths:
            - 'packages/happy-app/**'
            - '.github/workflows/typecheck.yml'
    push:
        branches:
            - main
        paths:
            - 'packages/happy-app/**'
            - '.github/workflows/typecheck.yml'

jobs:
    typecheck:
        runs-on: ubuntu-latest
        timeout-minutes: 15
        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Setup Node
              uses: actions/setup-node@v4
              with:
                  node-version: 22
                  cache: yarn

            - name: Install dependencies
              run: yarn install --frozen-lockfile
              env:
                  SKIP_HAPPY_SYNC_BUILD: '1'

            - name: Build happy-sync
              run: yarn workspace @slopus/happy-sync build

            - name: TypeScript typecheck
              run: yarn workspace happy-app typecheck
