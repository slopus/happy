All done. Here's a summary of what was completed:

## Step 11 — Dead Code Cleanup (completed)

### What was done

**Migrated legacy consumers to v3:**
- `useSessionUsage` — now reads from `session.latestUsage` instead of `reducerState.latestUsage`
- Voice hooks (`voiceHooks.ts`, `contextFormatters.ts`) — now use `v3.MessageWithParts` instead of legacy `Message`
- `isMutableToolCall` — now scans v3 messages for tool parts instead of using `reducerState.toolIdToMessageId`
- `applyV3Messages` — now handles EnterPlanMode detection (was in `applyMessages`)
- `useSessions()` consumers — migrated to `useAllSessions()` (returns `Session[]` directly, no string headers)
- `sync.ts` reconnection — now iterates `sessions` directly instead of `sessionsData`

**Removed legacy message processing from storage.ts:**
- Removed `SessionMessages` interface, `SessionListItem` type, `sessionsData` field
- Removed `sessionMessages` field from Zustand store
- Removed `applyMessages()`, `applyDirectMessages()`, `applyMessagesLoaded()` methods
- Removed `useSessions()` hook
- Removed AgentState → reducer processing from `applySessions()`
- Removed all reducer/`Message`/`NormalizedMessage` imports

**Removed legacy message processing from sync.ts:**
- Removed `enqueueMessages()`, `scheduleQueuedMessagesProcessing()`, `getSessionMessageLock()` 
- Removed `applyMessages()` private method
- Removed legacy message queue fields (`sessionMessageQueue`, `sessionQueueProcessing`, `sessionMessageLocks`)
- Simplified `fetchMessages()` to only track seq numbers (v3 handled by SyncNode)
- Simplified push notification handler to v3-only path

**Deleted files:**
- `packages/happy-app/sources/sync/reducer/` — entire folder (6 files)
- `packages/happy-app/sources/components/MessageView.tsx` — dead component
- `packages/happy-app/sources/hooks/useDemoMessages.ts` — legacy demo hook
- `packages/happy-app/sources/app/(app)/dev/messages-demo.tsx` — legacy demo page
- `packages/happy-app/sources/app/(app)/dev/messages-demo-data.ts` — legacy demo data
- `packages/happy-app/sources/sync/typesRaw.spec.ts` — legacy test

**Moved:**
- `activityUpdateAccumulator.ts` — from `reducer/` to `sync/` (still actively used, not legacy)

### Verification
- All 4 packages typecheck clean (happy-sync, happy-cli, happy-server, happy-app)
- 75 happy-sync unit tests pass
- 8 app ops.spec tests pass
- 21 activityUpdateAccumulator tests pass
- 24 CLI mapper tests pass (13 Claude + 11 Codex)
- 2 pre-existing CLI test failures (unrelated `@/utils/lock` path resolution)
