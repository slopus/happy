{
  "$schema": "../node_modules/@tauri-apps/cli/config.schema.json",
  "productName": "Runline (preview)",
  "identifier": "com.runline.arc.preview",
  "app": {
    "windows": [
      {
        "title": "Runline (preview)"
      }
    ]
  }
}
