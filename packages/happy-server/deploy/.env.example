# Happy Server - Environment Configuration
# Copy this to .env and fill in your values

# ===================
# Required Settings
# ===================

# API domain (e.g., happy-api.example.com)
API_DOMAIN=happy-api.example.com

# Master secret for signing tokens - generate with: openssl rand -hex 32
HANDY_MASTER_SECRET=

# PostgreSQL password - generate with: openssl rand -hex 16
POSTGRES_PASSWORD=

# S3/MinIO credentials - generate with: openssl rand -hex 16
S3_ACCESS_KEY=
S3_SECRET_KEY=

# Public URL for file downloads (must be accessible from clients)
# Example: https://happy-files.example.com/happy
S3_PUBLIC_URL=https://happy-files.example.com/happy

# ===================
# Optional Settings
# ===================

# PostgreSQL settings (defaults shown)
POSTGRES_USER=postgres
POSTGRES_DB=handy

# S3 bucket name (default: happy)
S3_BUCKET=happy

# S3 domain for Traefik (for public file access)
S3_DOMAIN=happy-files.example.com

# MinIO console domain (optional, for admin access)
S3_CONSOLE_DOMAIN=happy-minio.example.com

# Traefik network name (default: traefik)
TRAEFIK_NETWORK=traefik

# Traefik entrypoint for HTTPS (default: websecure)
TRAEFIK_ENTRYPOINT=websecure

# Traefik certificate resolver name (default: letsencrypt)
CERT_RESOLVER=letsencrypt

# Pre-built server image (optional, for production deployment)
# If not set, docker-compose will build from Dockerfile.server
# SERVER_IMAGE=ghcr.io/your-org/happy/happy-server:latest
