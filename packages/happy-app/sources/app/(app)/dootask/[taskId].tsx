import { View, Text } from 'react-native';

export default function DooTaskDetail() {
    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
            <Text>DooTask Detail — TODO</Text>
        </View>
    );
}
