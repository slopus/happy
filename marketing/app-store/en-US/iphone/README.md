# iPhone App Store screenshot drafts

These five 1320 × 2868 PNGs checkpoint the reviewed first composition before
the next copy/layout revision. They are drafts, not approved App Store uploads.
The collaboration card contains disclosed fictional participant content and
does not prove authenticated team sharing. The desktop companion is not an
iPhone feature screen. Verify all claims against the submitted app before use.

Keep only the currently selected image set here; earlier drafts remain in Git
history. Raw captures and intermediate exports stay in ignored `.context/`.
A separate real iPad capture set will live alongside this folder in `../ipad/`.